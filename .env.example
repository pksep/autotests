# Copy this file to .env and fill in real values. Do not commit .env.
# Used for UI login (setup.ts) and API auth (APIConstants, LOGIN_TEST_CONFIG).

LOGIN_TABEL=105
LOGIN_USERNAME=YourDisplayName
LOGIN_PASSWORD=your_password_here
