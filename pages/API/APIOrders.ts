import { APIRequestContext, Page } from '@playwright/test';
import { APIPageObject } from '../../lib/APIPage';
import logger from '../../lib/utils/logger';

/** Нет соответствующего контроллера в sep_erp_server — вызовы через {@link APIPageObject.apiProbe}. */
export class OrdersAPI extends APIPageObject {
  constructor(page: Page) {
    super(page);
  }

  async createOrder(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'createOrder', args };
    return this.apiProbe(request, 'OrdersAPI.createOrder', payload, accessToken);
  }

  async updateOrder(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'updateOrder', args };
    return this.apiProbe(request, 'OrdersAPI.updateOrder', payload, accessToken);
  }

  async getOrderById(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'getOrderById', args };
    return this.apiProbe(request, 'OrdersAPI.getOrderById', payload, accessToken);
  }

  async deleteOrder(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'deleteOrder', args };
    return this.apiProbe(request, 'OrdersAPI.deleteOrder', payload, accessToken);
  }

  async getAllOrders(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'getAllOrders', args };
    return this.apiProbe(request, 'OrdersAPI.getAllOrders', payload, accessToken);
  }

  async getOrdersByStatus(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'getOrdersByStatus', args };
    return this.apiProbe(request, 'OrdersAPI.getOrdersByStatus', payload, accessToken);
  }

  async updateOrderStatus(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'updateOrderStatus', args };
    return this.apiProbe(request, 'OrdersAPI.updateOrderStatus', payload, accessToken);
  }

  async getOrderItems(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'getOrderItems', args };
    return this.apiProbe(request, 'OrdersAPI.getOrderItems', payload, accessToken);
  }

  async addOrderItem(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'addOrderItem', args };
    return this.apiProbe(request, 'OrdersAPI.addOrderItem', payload, accessToken);
  }
}
