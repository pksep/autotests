import { APIRequestContext, Page } from '@playwright/test';
import { APIPageObject } from '../../lib/APIPage';
import logger from '../../lib/utils/logger';

/** Нет соответствующего контроллера в sep_erp_server — вызовы через {@link APIPageObject.apiProbe}. */
export class SearchAPI extends APIPageObject {
  constructor(page: Page) {
    super(page);
  }

  async searchGlobal(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'searchGlobal', args };
    return this.apiProbe(request, 'SearchAPI.searchGlobal', payload, accessToken);
  }

  async searchUsers(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'searchUsers', args };
    return this.apiProbe(request, 'SearchAPI.searchUsers', payload, accessToken);
  }

  async searchProducts(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'searchProducts', args };
    return this.apiProbe(request, 'SearchAPI.searchProducts', payload, accessToken);
  }

  async searchOrders(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'searchOrders', args };
    return this.apiProbe(request, 'SearchAPI.searchOrders', payload, accessToken);
  }

  async searchInventory(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'searchInventory', args };
    return this.apiProbe(request, 'SearchAPI.searchInventory', payload, accessToken);
  }

  async searchDocuments(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'searchDocuments', args };
    return this.apiProbe(request, 'SearchAPI.searchDocuments', payload, accessToken);
  }

  async searchTasks(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'searchTasks', args };
    return this.apiProbe(request, 'SearchAPI.searchTasks', payload, accessToken);
  }

  async searchContacts(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'searchContacts', args };
    return this.apiProbe(request, 'SearchAPI.searchContacts', payload, accessToken);
  }

  async searchEquipment(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'searchEquipment', args };
    return this.apiProbe(request, 'SearchAPI.searchEquipment', payload, accessToken);
  }

  async searchMaterials(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'searchMaterials', args };
    return this.apiProbe(request, 'SearchAPI.searchMaterials', payload, accessToken);
  }

  async getSearchSuggestions(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'getSearchSuggestions', args };
    return this.apiProbe(request, 'SearchAPI.getSearchSuggestions', payload, accessToken);
  }

  async getSearchHistory(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'getSearchHistory', args };
    return this.apiProbe(request, 'SearchAPI.getSearchHistory', payload, accessToken);
  }

  async clearSearchHistory(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'clearSearchHistory', args };
    return this.apiProbe(request, 'SearchAPI.clearSearchHistory', payload, accessToken);
  }

  async getSearchStatistics(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'getSearchStatistics', args };
    return this.apiProbe(request, 'SearchAPI.getSearchStatistics', payload, accessToken);
  }
}
