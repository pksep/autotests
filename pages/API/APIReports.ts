import { APIRequestContext, Page } from '@playwright/test';
import { APIPageObject } from '../../lib/APIPage';
import logger from '../../lib/utils/logger';

/** Нет соответствующего контроллера в sep_erp_server — вызовы через {@link APIPageObject.apiProbe}. */
export class ReportsAPI extends APIPageObject {
  constructor(page: Page) {
    super(page);
  }

  async createReport(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'createReport', args };
    return this.apiProbe(request, 'ReportsAPI.createReport', payload, accessToken);
  }

  async updateReport(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'updateReport', args };
    return this.apiProbe(request, 'ReportsAPI.updateReport', payload, accessToken);
  }

  async getReportById(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'getReportById', args };
    return this.apiProbe(request, 'ReportsAPI.getReportById', payload, accessToken);
  }

  async deleteReport(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'deleteReport', args };
    return this.apiProbe(request, 'ReportsAPI.deleteReport', payload, accessToken);
  }

  async getAllReports(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'getAllReports', args };
    return this.apiProbe(request, 'ReportsAPI.getAllReports', payload, accessToken);
  }

  async getReportsByType(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'getReportsByType', args };
    return this.apiProbe(request, 'ReportsAPI.getReportsByType', payload, accessToken);
  }

  async generateReport(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'generateReport', args };
    return this.apiProbe(request, 'ReportsAPI.generateReport', payload, accessToken);
  }

  async getReportStatus(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'getReportStatus', args };
    return this.apiProbe(request, 'ReportsAPI.getReportStatus', payload, accessToken);
  }

  async downloadReport(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'downloadReport', args };
    return this.apiProbe(request, 'ReportsAPI.downloadReport', payload, accessToken);
  }

  async scheduleReport(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'scheduleReport', args };
    return this.apiProbe(request, 'ReportsAPI.scheduleReport', payload, accessToken);
  }

  async getReportSchedule(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'getReportSchedule', args };
    return this.apiProbe(request, 'ReportsAPI.getReportSchedule', payload, accessToken);
  }

  async updateReportSchedule(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'updateReportSchedule', args };
    return this.apiProbe(request, 'ReportsAPI.updateReportSchedule', payload, accessToken);
  }

  async deleteReportSchedule(request: APIRequestContext, ...args: any[]) {
    const last = args[args.length - 1];
    const accessToken =
      typeof last === 'string' && last !== 'invalid_user' && (last.startsWith('ey') || last.startsWith('Bearer'))
        ? (args.pop(), last)
        : undefined;
    const payload = { method: 'deleteReportSchedule', args };
    return this.apiProbe(request, 'ReportsAPI.deleteReportSchedule', payload, accessToken);
  }
}
