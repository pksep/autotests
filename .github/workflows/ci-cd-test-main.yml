name: CI/CD for Main Branch

on:
  pull_request:
    branches:
      - CI-CD-Test-Main

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '22.13.1'

      - name: Install dependencies
        run: npm install

      - name: Install Playwright dependencies
        run: sudo npx playwright install-deps

      - name: Install Playwright browsers
        run: npx playwright install

      - name: Run Playwright tests with xvfb
        env:
          TEST_DIR: '.'
          BASE_URL: 'http://dev.npoamotiv.ru'
        run: xvfb-run --auto-servernum --server-args="-screen 0 1920x1080x24" npm run test

      - name: Generate Allure report
        if: always()
        run: |
          npm install -g allure-commandline --save-dev
          allure generate --clean

      - name: List Allure report directory
        if: always()
        run: ls -la allure-report

      - name: List current working directory
        if: always()
        run: ls -la

      - name: Configure git
        if: always()
        run: |
          git config --global user.email "rgjoyce111@gmail.com"
          git config --global user.name "atomant1969"

      - name: Deploy Allure report to GitHub Pages
        if: always()
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git remote set-url origin https://$GH_PAT@github.com/npo-automotive/autotests.git
          npm install -g gh-pages
          gh-pages -d allure-report -u "github-actions-bot <support+actions@github.com>"
